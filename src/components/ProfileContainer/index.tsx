import { ProfileContainerStyled } from './styles';

export const ProfileContainer = ({ children }: { children: React.ReactNode }) => {
  return <ProfileContainerStyled>{children}</ProfileContainerStyled>;
};
