export type User = {
  name: string;
  profession: string;
  city: string;
  reviews: number;
  price: number;
  timebox: string;
  description: string;
  profilePicture: string;
};

export type Calendar = {};
